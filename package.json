{
  "name": "name",
  "scripts": {
    "conv": "bun run --filter conv dev",
    "web": "bun run --filter web dev",
    "check": "bun run --filter conv check && bun run --filter web check"
  },
  "workspaces": [
    "conv",
    "web"
  ],
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^25.6.0",
    "vite-plus": "^0.1.18"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@better-auth/api-key": "^1.6.9",
    "@convex-dev/better-auth": "^0.11.5",
    "@faker-js/faker": "^10.4.0",
    "ai": "^6.0.168",
    "better-auth": "^1.6.7",
    "zod": "^4.3.6"
  }
}